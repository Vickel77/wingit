import React from "react";

export default function Facebook({ width }) {
  return (
    <img
      src="facebook.png"
      alt=""
      width={width}
      style={{ filter: "invert(1)" }}
    />
  );
}
